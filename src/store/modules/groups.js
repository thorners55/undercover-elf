//import axios from "axios";

const state = {
  groups: [],
};

const getters = {
  getGroups: (state) => state.groups,
};

const actions = {};

const mutations = {};

export default { state, getters, actions, mutations };
