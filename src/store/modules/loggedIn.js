const state = {
  isLoggedIn: false,
};

/*const mutations = {
  logInOrOut(state) {
    state.loggedIn = !state.loggedIn;
  },
};

const actions = {
  logInOrOut(context) {
    context.commit("logInOrOut");
  },
};*/

export default { state };
