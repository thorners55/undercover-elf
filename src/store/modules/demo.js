import router from "../../router";

const namespaced = true;

const state = {
  groups: [
    {
      name: "Dumbledore's Army",
      groupId: "dum813d0r3s4rmy",
    },
    { name: "Gryffindor Quidditch Team", groupId: "qu1dd1tch" },
  ],
};

const getters = {};

const mutations = {};

const actions = {};

export default { state, getters, actions, mutations, namespaced };
